<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.diet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme.FullScreen">
        <activity android:name=".Diyet">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".YKarisimEkleSil" />
        <activity android:name=".YiyecekEkleSil" />
        <activity android:name=".Bugun" />
        <activity android:name=".SonBirAy" />
        <activity android:name=".KayitAktar" />
        <activity android:name=".Ayarlar" />

    </application>

</manifest>